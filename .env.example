# Example Environment Configuration for vLLM Integration

# =============================================================================
# CUSTOM vLLM CONFIGURATION (OpenAI-Compatible API)
# =============================================================================
# To use your custom vLLM model (gptoss20b) instead of OpenAI's API:
# 1. Set OPENAI_BASE_URL to your vLLM endpoint (include /v1 suffix)
# 2. Set OPENAI_MODEL_NAME to your model path
# 3. Set OPENAI_API_KEY to any value (vLLM doesn't require real authentication)

# Option 1: Direct connection to vLLM server
OPENAI_BASE_URL=http://103.196.86.194:8000/v1
OPENAI_MODEL_NAME=/mnt/models/gptoss20b_merged_bf16
OPENAI_API_KEY=dummy-key

# Option 2: SSH tunnel (more secure - recommended)
# First run: ssh -L 8000:localhost:8000 root@103.196.86.194 -p 53482 -N
# Then use:
# OPENAI_BASE_URL=http://localhost:8000/v1
# OPENAI_MODEL_NAME=/mnt/models/gptoss20b_merged_bf16
# OPENAI_API_KEY=dummy-key

# =============================================================================
# STANDARD OPENAI API CONFIGURATION
# =============================================================================
# To use OpenAI's API instead, comment out OPENAI_BASE_URL and set your real API key:
# OPENAI_API_KEY=sk-your-actual-openai-api-key-here

# =============================================================================
# OTHER LLM PROVIDERS
# =============================================================================
# Groq API configuration
GROQ_API_KEY=your-groq-api-key-here

# Google Vertex AI / Gemini configuration
# GOOGLE_APPLICATION_CREDENTIALS_JSON_B64=your-base64-encoded-service-account-json

# =============================================================================
# APPLICATION SETTINGS
# =============================================================================
# Enable prompt logging for debugging
LOG_PROMPTS=false
PROMPT_LOG_FILE=groq_prompts.log

# Number of retry attempts for LLM requests
LLM_RETRY_ATTEMPTS=3

# Training data collection
# ENABLE_TRAINING_DATA_COLLECTION=true
# TRAINING_DATA_FILE=training_data.jsonl
